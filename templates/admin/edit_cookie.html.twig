{% extends "admin/formLayout.html.twig" %}

{% block main %}

    <h1>{{ title }}</h1>

    <div class="container">
        <div class="row">
            <div class="col-xs-12">

                {{ include('admin/_form.html.twig', {
                    'buttonName': 'Edit'
                }) }}

            </div>
        </div>
    </div>

{% endblock main %}

